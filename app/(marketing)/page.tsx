/**
 * app/(marketing)/page.tsx
 *
 * Ce fichier déclare la route `/` dans le groupe (marketing).
 * En présence de app/page.tsx, Next.js utilise app/page.tsx pour `/`.
 * Ce fichier exporte un composant minimal pour satisfaire AppPageConfig.
 */
export default function MarketingRootPlaceholder() {
  return null
}
